{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "name": "myapp-web",
  "buildCommand": "npm run build:shared --verbose && npm run build:web --verbose",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --no-optional --force",
  "framework": "nextjs",
  "build": {
    "env": {
      "NODE_VERSION": "18"
    }
  }
}
