{
  "buildCommand": "npm install --prefix ../.. --no-optional --force && npm run build:shared --prefix ../.. && npm run build --prefix ../..",
  "framework": "nextjs",
  "build": {
    "env": {
      "NODE_VERSION": "18"
    }
  }
}
