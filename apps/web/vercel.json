{
  "installCommand": "npm install --prefix ../.. --no-optional --force",
  "buildCommand": "npm run build:shared --prefix ../.. && npm run build:web --prefix ../..",
  "framework": "nextjs",
  "build": {
    "env": {
      "NODE_VERSION": "18"
    }
  }
}
